import styled from "styled-components"

export const StyledFieldDescriptor = styled.div`
    display: flex;
    flex-direction: column;
    gap: 4px;
    color: #274060;

    strong{
        font-size: 14px;
    }

    span{
        font-size: 12px;
        font-weight: 700;
        text-transform: lowercase;
    }
`;